# TODOs

Open follow-up items captured during the Clients page test work.

- `clients-edit-uri-hydration-observability.md`: `clients-edit.js` can fail silently while the page still renders.
- `clients-edit-stable-e2e-selectors.md`: the Clients edit page lacks stable UI hooks for resilient Playwright tests.
- `clients-edit-request-aborted-propagation.md`: `Clients/Edit` does not pass `RequestAborted` to `IClientAdminService`.
- `apiscopes-e2e-obsolete-navigation-api.md`: an existing API scopes browser test still uses an obsolete Playwright API.
