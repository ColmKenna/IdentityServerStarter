@page
@model PrivacyModel
@{
    ViewData["Title"] = "Access guidance";
}

<h1>@ViewData["Title"]</h1>

<p>This app uses claim-based authorization. A denied page means the signed-in user is missing the required claim.</p>
